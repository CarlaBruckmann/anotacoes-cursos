/*Faça um programa que solicite a base e altura de um retângulo e informe sua área e perímetros.
Onde temos o seguinte:
area = base * altura
perimetro = (base * 2) + (altura * 2)
*/
let base: number = 90
let altura: number = 80
let area: number = base * altura
let perimetro: number = (base * 2) + (altura * 2)
console.log(area, perimetro)//7200 340